# مهام معلقة / مستقبلية (لا ننسى)

تم تنفيذ التحسينات المتبقية حسب الأصح برمجياً (بدون تكرار الإعداد).

---

## ✅ تم تنفيذه الآن (حسب الأصح برمجياً)

1. **تقليل المهلة القصوى لانتظار Storage** من 10 إلى **8 ثوانٍ** في مسار رابط الإداري + db فارغ — `app.js` (maxWaitMs = 8000).
2. **تقليل التأخير بين محاولات جلب الفترة** من 1.5 إلى **1 ثانية** — `app.js` (بين كل محاولة من الثلاث: 1000ms بدل 1500ms).

---

## ✅ تم تنفيذها لاحقاً

| # | المهمة | الملاحظة |
|---|--------|--------|
| 1 | **تهيئة Firebase مبكرة من الـ head** | تم: مصدر واحد `src/firebase-config.js` + سكريبت inline في الـ head بعد SDK؛ `app.js` يستخدم `window.firebaseConfig` و`window.storage` إن وُجدا. راجع `TEST_CHECKLIST_EARLY_FIREBASE.md`. |

---

## مرجع سريع

- **مراجعة التدفقات والأداء:** `SENIOR_REVIEW_FLOWS_AND_PERFORMANCE.md`
- **التحسينات المُنفذة سابقاً:** `SENIOR_IMPROVEMENTS_APPLIED.md`
