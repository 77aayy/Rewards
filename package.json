{
  "name": "rewards",
  "version": "1.0.0",
  "private": true,
  "description": "نظام مكافآت فندق إليت - موقع ستاتيك",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "npx serve -s dist -l 8080",
    "storage-cors": "node scripts/set-storage-cors.js",
    "test:e2e": "playwright test e2e/e2e-full.spec.js --project=chromium"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "tailwindcss": "^3.4.1"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.14.0",
    "serve": "^14.2.0"
  }
}
